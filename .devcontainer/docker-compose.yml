version: '3.8'
services:
  r-dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
      - ~/.gitconfig:/home/myuser/.gitconfig:ro
    command: sleep infinity
    environment:
      - DISPLAY=${DISPLAY}
    user: myuser
    working_dir: /workspace
